package msc.jkuat.mobile.web.ui;

import org.apache.log4j.Logger;

//import common.Logger;

public class Forex extends ForwardComposer {
	@SuppressWarnings("unused")
	private Logger log = Logger.getLogger(getClass());

	/*public void onClick$lblHome() {
		String home = (String) self.getAttribute("home");
		self.detach();
		@SuppressWarnings("unused")
		Window wnd = (Window) Executions.createComponents(home, this.self.getParent(), null);
		//log .debug(home + " created");
	}*/
}
